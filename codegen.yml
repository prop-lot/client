# Used to generate API graphql types
overwrite: true
schema: "./graphql/schemas/schema.graphql"
generates:
  ./graphql/server/generated.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
